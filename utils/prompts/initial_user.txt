The details about observation space (states array) and action space (actions array) is as follows {task_obs_code_string}. You can use the above information to access any particular state, action (for example states[i], actions[j] to access ith state, jth action respectively) and write a reward function for the following task: {task_description}.